@model IEnumerable<WardrobeMVC.Models.Sho>

@{
    ViewBag.Title = "Index";
}

<h2>Shoes</h2>

@*CREATE NEW SHOES*@
<p class="new-item">
    <button type="button" class="btn btn-info create-new" onclick="location.href='@Url.Action("Create", "Shoes")'">New Shoes</button>
</p>

@*ROW CONTAINING SHOES INFORMATION*@
<div class="row">
    @*LOOP THAT IS ABLE TO HANDLE NEW SHOES ADDITIONS*@
    @foreach (var item in Model)
    {
        @*SHOES*@
        <div class="col-sm-4 col-md-4">
            <div class="thumbnail">
                <div class="caption">
                    <div class="row">
                        <div class="col-md-12">
                            <h4>@Html.DisplayFor(modelItem => item.ShoeName)</h4>
                            <p><img src="@Html.DisplayFor(modelItem => item.ShoeImage)" class="img-responsive image" /></p>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6 description">
                            <p>Occasion:&nbsp;<span>@Html.DisplayFor(modelItem => item.Occasion.OccasionName)</span></p>
                            <p>Season:&nbsp;<span>@Html.DisplayFor(modelItem => item.Season.SeasonName)</span></p>
                        </div>
                        <div class="col-md-6 buttons">
                            @*BUTTONS FOR DETAILS AND EDIT*@
                            <p>@Html.ActionLink("Details", "Details", new { id = item.ShoeID }, new { @class = "btn btn-info index", role = "button" })</p>
                            <p>@Html.ActionLink("Edit", "Edit", new { id = item.ShoeID }, new { @class = "btn btn-info index", role = "button" })</p>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    }
</div>
